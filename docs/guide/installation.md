# Installation

Packages
Install needed packages via.


``` bash
  yay -S noon-main noon-libqt
```
This will install the needed shell files
``` bash
 noon-setup 
```

# Removal (Just remove the packages) 
``` bash
  yay -Rcns noon-main noon-libqt
```
